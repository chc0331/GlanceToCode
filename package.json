{
  "name": "design-to-code",
  "version": "1.0.0",
  "private": true,
  "description": "Figma plugin: Design to Code (Rectangle -> Box)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.101.0",
    "typescript": "^5.6.3"
  }
}

 